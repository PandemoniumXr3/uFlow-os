/**
 * Centralized placeholder — a real support inbox has not been supplied yet. `example.com` is the
 * IANA-reserved documentation domain, so this can never resolve to a real mailbox. Replace this one
 * constant (not the individual call sites) once a real address exists.
 */
export const SUPPORT_EMAIL = 'uflow-support@example.com';
